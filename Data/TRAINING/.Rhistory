.5^(3/2)
13110 / .1111
13110 / .1111 * 1.02
15757 / .1111
141827.2 - 120361.8
21465.4 / 12
60000/.8
sqrt(.5)
devtools::install_github('jdwilson4/multilayer_extraction')
library(MultilayerExtraction, quietly = TRUE)
?multilayer.extraction
data("AU_CS")
n <- dim(AU_CS[[1]])[1]
m <- length(AU_CS)
n
n <- dim(AU_CS[[1]])[1]
n
m <- length(AU_CS)
m
relationship.names <- names(AU_CS)
print(relationship.names)
image(AU_CS$coauthor, main = "Co-author")
image(AU_CS$facebook, main = "Leisure")
image(AU_CS$leisure, main = "Facebook")
Mean.Degree <- sapply(AU_CS, mean) * n
print(Mean.Degree)
community.object <- multilayer.extraction(adjacency = AU_CS, seed = 123, min.score = 0, prop.sample = 1.00)
plot(community.object, main = "Diagnostic Plot AU_CS")
object <- refine(community.object, k = 6, m = m, n = n)
object
print(data.frame(Layers = num.layers, Vertices = num.vertices))
num.layers <- colSums(object$Layers > 0)
num.vertices <- colSums(object$Vertices)
print(data.frame(Layers = num.layers, Vertices = num.vertices))
indx1 <- which(object$Vertices[,1] == 1)
indx2 <- setdiff(which(object$Vertices[,2] == 1), indx1)
indx3 <- setdiff(setdiff(which(object$Vertices[,3] == 1), indx1), indx2)
indx4 <- setdiff(setdiff(setdiff(which(object$Vertices[,4] == 1), indx3),       indx2),indx1)
indx5 <- setdiff(setdiff(setdiff(setdiff(which(object$Vertices[,5] == 1), indx4), indx3),indx2), indx1)
indx6 <- setdiff(setdiff(setdiff(setdiff(setdiff(which(object$Vertices[,6] == 1),
indx1), indx2), indx3), indx4), indx5)
none2 <- setdiff(1:61, union(union(union(indx1, indx2), union(indx3, indx4)), union(indx5, indx6)))
image(object$Layers, main = "Layers in Communities")
re.order <- c(indx1, indx2, indx3, indx4, indx5, indx6, none2)
image(AU_CS$coauthor[re.order,re.order], main = "Co-Author")
image(AU_CS$leisure[re.order,re.order], main = "Leisure")
image(AU_CS$work[re.order,re.order], main = "Work")
image(AU_CS$lunch[re.order,re.order], main = "Lunch")
image(AU_CS$facebook[re.order,re.order], main = "Facebook")
data <- scan("")
hist(data, n = 50)
hist(data[data != 0], n = 50)
hist(data[data != 0], n = 50, title = "Quiz Average after Quiz 2")
hist(data[data != 0], n = 50, main = "Quiz Average", xlab = "Grade")
??gergm
install.packages("gergm")
install.packages("GERGM")
library(GERGM)
?gergm
data <- read.txt("/Users/jdwilson4/Dropbox/Teaching/USF/Regression_Analysis/Case Study/train.txt")
data <- read.table("/Users/jdwilson4/Dropbox/Teaching/USF/Regression_Analysis/Case Study/train.txt")
data <- read.table("/Users/jdwilson4/Dropbox/Teaching/USF/Regression_Analysis/Case Study/train.txt", header = TRUE)
data <- read.table("/Users/jdwilson4/Dropbox/Teaching/USF/Regression_Analysis/Case Study/train.txt", header = TRUE, sep = ",")
data$SalePrice
data2 <- read.table("/Users/jdwilson4/Dropbox/Teaching/USF/Regression_Analysis/Case Study/test.txt", header = TRUE, sep = ",")
View(data)
indx <- which.min(data$SalePrice)
indx
data[496, ]
data.point <- data[6, ]
data.point
summary(data$SalePrice)
write.table(data.point, file = "/Users/jdwilson4/Desktop/Morty.txt")
write.csv(data.point, file = "/Users/jdwilson4/Desktop/Morty.txt", header = TRUE)
write.csv(data.point, file = "/Users/jdwilson4/Desktop/Morty.txt")
read.table(file = "/Users/jdwilson4/Desktop/Morty.txt", sep = ",", header = TRUE)
devtools::install_github("jdwilson4/ESSC")
library(ESSC, quietly = TRUE)
library(Matrix, quietly = TRUE)
library(Rlab, quietly = TRUE)
library(devtools, quietly = TRUE)
FB <- read.csv(file = "https://github.com/jdwilson4/ESSC/blob/master/data/Personal_FB_Edgelist.csv",
header = TRUE)
FB <- read.csv(file = "https://raw.githubusercontent.com/jdwilson4/ESSC/master/data/Personal_FB_Edgelist.csv",
header = TRUE)
FB.edgelist <- FB[, 1:2]
library(igraph)
FB.graph <- graph_from_edgelist(FB.edgelist, directed = FALSE)
FB.edgelist <- as.matrix(FB[, 1:2])
FB.graph <- graph_from_edgelist(FB.edgelist, directed = FALSE)
FB.adj <- get.adjacency(FB.graph)
library(Matrix)
image(Matrix(FB.adj))
dev.new()
image(FB.adj)
image(matrix(FB.adj))
rowSums(FB.adj)
which(FB.adj[1, ] == 1)
which(FB.adj[, 1] == 1)
FB.adj2 <- as.matrix(FB.adj)
which(FB.adj2[, 1] == 1)
FB.adj2[,1]
FB.adj2[,2]
which(FB.adj2[, 1] > 0)
devtools::install_github("jdwilson4/ESSC")
library(ESSC, quietly = TRUE)
library(Matrix, quietly = TRUE)
library(Rlab, quietly = TRUE)
library(devtools, quietly = TRUE)
?essc
library(ESSC, quietly = TRUE)
library(Matrix, quietly = TRUE)
library(Rlab, quietly = TRUE)
net <- stochastic.block(n = 1000, k = 3, P = cbind(c(0.1, 0.01, 0.01), c(0.01, 0.1, 0.01), c(0.01, 0.01, 0.1)), sizes = c(300, 300, 400))
image(net$Adjacency)
results.pois <- essc(net$Adjacency, alpha = 0.10, Null = "Poisson")
print(results.bin$Communities)
print(results.pois$Communities)
names(results.pois)
results.pois$Background
results.pois <- essc(net$Adjacency, alpha = 0.10, Null = "Poisson")
print(results.pois$Communities)
names(results.pois)
essc = function(Adj.Matrix, alpha, Null = c("Binomial", "Poisson"), Num.Samples = nrow(Adj.Matrix)){
symdiff = function(X,Y){setdiff(union(X,Y),intersect(X,Y))}
degrees <- rowSums(Adj.Matrix)
n <- length(degrees)
index <- 1:dim(Adj.Matrix)[1]
extractFrom <- sample(index, Num.Samples, replace = FALSE)
#initialize
Community <- rep(list(NULL), Num.Samples)
pvals <- rep(list(NULL), Num.Samples)
unq <- TRUE
nodes <- matrix(0, 2, Num.Samples)
nodes[1, ] <- extractFrom
which_match <- 0
i <- 1
count <- 0
#Running long loop
for(node in extractFrom){
count <- count+1
#cat("######################################################\n")
#cat(paste0("extraction ",count,"\n"))
B0 <- c(node,which(Adj.Matrix[node,]>0))
temp <- Main.Search(Adj.Matrix, alpha, B0, Null)
if(length(temp$Community) > 1){
if(i > 1){
unq <- TRUE
which_match <- 0
for(j in 1: i){
if(length(symdiff(Community[[j]], temp$Community)) == 0){
unq <- FALSE
which_match <- j
}
}
}
if(unq){
Community[[i]] <- temp$Community
pvals[[i]] <- temp$PValues
nodes[2, count] <- i
i = i+1
} else{
nodes[2, count] <- which_match
}
} else{
nodes[2, count] <- 0
}
}
Community <- Community[unlist(lapply(Community, function(comm) length(comm) > 0))]
Background <- setdiff(1:n, unlist(Community))
pvals <- pvals[unlist(lapply(pvals, function(comm) length(comm) > 0))]
pvals <- data.frame(matrix(unlist(pvals), nrow = n, byrow = T), stringsAsFactors = FALSE)
if(length(Community) == 0){
Community <- list(NULL)
pvals <- NULL
}
return(list(Communities = Community, Background = Background, PValues = pvals))
}
###Single Search Function
Main.Search = function(Adj.Matrix,alpha,B0, Null){
j <- 0
degrees <- rowSums(Adj.Matrix)
n <- length(degrees)
#Ensuring B1 and B0 are not equal to start the loop
B1 <- integer(0)
#Function for checking equality of matrices
vectorequal = function(x,y){
is.numeric(x) && is.numeric(y) && length(x) == length(y) && all(x==y)
}
#cat("###### Starting Main.Search Loop ######\n")
while(vectorequal(B0,B1) == FALSE & j <= 30){
j <- j + 1
if(j < 5||j%%5==0)
#cat(paste("iteration",j,"\n"))
if(j > 1)
B0 <- B1
if(length(B0) > 1)
duBs <- rowSums(Adj.Matrix[, B0])
if(length(B0)==1)
duBs <- Adj.Matrix[, B0]
pB <- sum(degrees[B0])/sum(degrees) #probability of connection to B
if(Null == "Binomial")
pvals <- pbinom(duBs, degrees, pB, lower.tail = FALSE)
if(Null == "Poisson")
pvals <- ppois(duBs, degrees*pB, lower.tail = FALSE)
pvals[degrees == 0] <- 1
#cat(paste0("length pvals is ",length(pvals),"; mean is ",round(mean(pvals),2),"\n"))
pvals_bh <- pvals*n/rank(pvals)
if(sum(pvals_bh <= alpha) == 0)
{
B1 <- integer(0)
#cat("B1 reached size 0 in Main.Search\n")
break
}
threshold <- max(pvals[pvals_bh <= alpha])
B1 <- which(pvals <= threshold)
if(length(B1) < 1 ){break}
}
Community <- B1
if(j > 30){
#cat("Main.Search failed to converge before 30 iterations\n")
Community <- integer(0)
}
return(list(Community = Community, PValues = pvals))
}
results.pois <- essc(net$Adjacency, alpha = 0.10, Null = "Poisson")
results.pois$PValues
heatmap(results.pois$PValues)
heatmap(matrix(results.pois$PValues))
heatmap(as.matrix(results.pois$PValues))
?heatmap
d <- kmeans(results.pois$PValues, 3)
d
d$cluster
essc = function(Adj.Matrix, alpha, Null = c("Binomial", "Poisson"), Num.Samples = nrow(Adj.Matrix)){
symdiff = function(X,Y){setdiff(union(X,Y),intersect(X,Y))}
degrees <- rowSums(Adj.Matrix)
n <- length(degrees)
index <- 1:dim(Adj.Matrix)[1]
extractFrom <- sample(index, Num.Samples, replace = FALSE)
#initialize
Community <- rep(list(NULL), Num.Samples)
pvals <- rep(list(NULL), Num.Samples)
unq <- TRUE
nodes <- matrix(0, 2, Num.Samples)
nodes[1, ] <- extractFrom
which_match <- 0
i <- 1
count <- 0
#Running long loop
for(node in extractFrom){
count <- count+1
#cat("######################################################\n")
#cat(paste0("extraction ",count,"\n"))
B0 <- c(node,which(Adj.Matrix[node,]>0))
temp <- Main.Search(Adj.Matrix, alpha, B0, Null)
if(length(temp$Community) > 1){
if(i > 1){
unq <- TRUE
which_match <- 0
for(j in 1: i){
if(length(symdiff(Community[[j]], temp$Community)) == 0){
unq <- FALSE
which_match <- j
}
}
}
if(unq){
Community[[i]] <- temp$Community
pvals[[i]] <- temp$PValues
nodes[2, count] <- i
i = i+1
} else{
nodes[2, count] <- which_match
}
} else{
nodes[2, count] <- 0
}
}
Community <- Community[unlist(lapply(Community, function(comm) length(comm) > 0))]
Background <- setdiff(1:n, unlist(Community))
pvals <- pvals[unlist(lapply(pvals, function(comm) length(comm) > 0))]
pvals <- data.frame(matrix(unlist(pvals), nrow = n, byrow = T), stringsAsFactors = FALSE)
if(length(Community) == 0){
Community <- list(NULL)
pvals <- NULL
}
return(list(Communities = Community, Background = Background, PValues = pvals))
}
###Single Search Function
Main.Search = function(Adj.Matrix,alpha,B0, Null){
j <- 0
degrees <- rowSums(Adj.Matrix)
n <- length(degrees)
#Ensuring B1 and B0 are not equal to start the loop
B1 <- integer(0)
#Function for checking equality of matrices
vectorequal = function(x,y){
is.numeric(x) && is.numeric(y) && length(x) == length(y) && all(x==y)
}
#cat("###### Starting Main.Search Loop ######\n")
while(vectorequal(B0,B1) == FALSE & j <= 30){
j <- j + 1
if(j < 5||j%%5==0)
#cat(paste("iteration",j,"\n"))
if(j > 1)
B0 <- B1
if(length(B0) > 1)
duBs <- rowSums(Adj.Matrix[, B0])
if(length(B0)==1)
duBs <- Adj.Matrix[, B0]
pB <- sum(degrees[B0])/sum(degrees) #probability of connection to B
if(Null == "Binomial")
pvals <- pbinom(duBs, degrees, pB, lower.tail = FALSE)
if(Null == "Poisson")
pvals <- ppois(duBs, degrees*pB, lower.tail = FALSE)
pvals[degrees == 0] <- 1
#cat(paste0("length pvals is ",length(pvals),"; mean is ",round(mean(pvals),2),"\n"))
pvals_bh <- pvals*n/rank(pvals)
if(sum(pvals_bh <= alpha) == 0)
{
B1 <- integer(0)
#cat("B1 reached size 0 in Main.Search\n")
break
}
threshold <- max(pvals[pvals_bh <= alpha])
B1 <- which(pvals <= threshold)
if(length(B1) < 1 ){break}
}
Community <- B1
if(j > 30){
#cat("Main.Search failed to converge before 30 iterations\n")
Community <- integer(0)
}
return(list(Community = Community, PValues = pvals_bh))
}
results.pois <- essc(net$Adjacency, alpha = 0.10, Null = "Poisson")
print(results.pois$Communities)
heatmap(as.matrix(results.pois$PValues))
d <- kmeans(results.pois$PValues, 3)$cluster
d
print(results.pois$Communities)
results.pois$PValues[, 1]
pvals <- results.pois$PValues
tot.values <- rowSums(pvals)
normalized_pvals <- pvals / tot.values
normalized_pvals
rowSums(normalized_pvals)
heatmap(as.matrix(normalized_pvals))
essc = function(Adj.Matrix, alpha, Null = c("Binomial", "Poisson"), Num.Samples = nrow(Adj.Matrix)){
symdiff = function(X,Y){setdiff(union(X,Y),intersect(X,Y))}
degrees <- rowSums(Adj.Matrix)
n <- length(degrees)
index <- 1:dim(Adj.Matrix)[1]
extractFrom <- sample(index, Num.Samples, replace = FALSE)
#initialize
Community <- rep(list(NULL), Num.Samples)
pvals <- rep(list(NULL), Num.Samples)
unq <- TRUE
nodes <- matrix(0, 2, Num.Samples)
nodes[1, ] <- extractFrom
which_match <- 0
i <- 1
count <- 0
#Running long loop
for(node in extractFrom){
count <- count+1
#cat("######################################################\n")
#cat(paste0("extraction ",count,"\n"))
B0 <- c(node,which(Adj.Matrix[node,]>0))
temp <- Main.Search(Adj.Matrix, alpha, B0, Null)
if(length(temp$Community) > 1){
if(i > 1){
unq <- TRUE
which_match <- 0
for(j in 1: i){
if(length(symdiff(Community[[j]], temp$Community)) == 0){
unq <- FALSE
which_match <- j
}
}
}
if(unq){
Community[[i]] <- temp$Community
pvals[[i]] <- temp$PValues
nodes[2, count] <- i
i = i+1
} else{
nodes[2, count] <- which_match
}
} else{
nodes[2, count] <- 0
}
}
Community <- Community[unlist(lapply(Community, function(comm) length(comm) > 0))]
Background <- setdiff(1:n, unlist(Community))
pvals <- pvals[unlist(lapply(pvals, function(comm) length(comm) > 0))]
pvals <- matrix(unlist(pvals), nrow = n, byrow = T)
#divide each of these by the sum of the p-values for each community
tot.values <- rowSums(pvals)
normalized_pvals <- pvals / tot.values
if(length(Community) == 0){
Community <- list(NULL)
pvals <- NULL
}
return(list(Communities = Community, Background = Background,
PValues = normalized_pvals))
}
###Single Search Function
Main.Search = function(Adj.Matrix,alpha,B0, Null){
j <- 0
degrees <- rowSums(Adj.Matrix)
n <- length(degrees)
#Ensuring B1 and B0 are not equal to start the loop
B1 <- integer(0)
#Function for checking equality of matrices
vectorequal = function(x,y){
is.numeric(x) && is.numeric(y) && length(x) == length(y) && all(x==y)
}
#cat("###### Starting Main.Search Loop ######\n")
while(vectorequal(B0,B1) == FALSE & j <= 30){
j <- j + 1
if(j < 5||j%%5==0)
#cat(paste("iteration",j,"\n"))
if(j > 1)
B0 <- B1
if(length(B0) > 1)
duBs <- rowSums(Adj.Matrix[, B0])
if(length(B0)==1)
duBs <- Adj.Matrix[, B0]
pB <- sum(degrees[B0])/sum(degrees) #probability of connection to B
if(Null == "Binomial")
pvals <- pbinom(duBs, degrees, pB, lower.tail = FALSE)
if(Null == "Poisson")
pvals <- ppois(duBs, degrees*pB, lower.tail = FALSE)
pvals[degrees == 0] <- 1
#cat(paste0("length pvals is ",length(pvals),"; mean is ",round(mean(pvals),2),"\n"))
pvals_bh <- pvals*n/rank(pvals)
if(sum(pvals_bh <= alpha) == 0)
{
B1 <- integer(0)
#cat("B1 reached size 0 in Main.Search\n")
break
}
threshold <- max(pvals[pvals_bh <= alpha])
B1 <- which(pvals <= threshold)
if(length(B1) < 1 ){break}
}
Community <- B1
if(j > 30){
#cat("Main.Search failed to converge before 30 iterations\n")
Community <- integer(0)
}
return(list(Community = Community, PValues = pvals_bh))
}
devtools::install_github("jdwilson4/ESSC")
library(ESSC, quietly = TRUE)
net <- stochastic.block(n = 1000, k = 3, P = cbind(c(0.1, 0.01, 0.01), c(0.01, 0.1, 0.01), c(0.01, 0.01, 0.1)), sizes = c(300, 300, 400))
#view the network
image(net$Adjacency)
#Run ESSC with Poisson null
results.pois <- essc(net$Adjacency, alpha = 0.10, Null = "Poisson")
file.names <- list.files("/Users/jdwilson4/Desktop/CSDMC2010_SPAM/TRAINING")
training.emails <- list()
install.packages("EML")
training.directory <- "/Users/jdwilson4/Desktop/CSDMC2010_SPAM/TRAINING"
test.directory <- "/Users/jdwilson4/Desktop/CSDMC2010_SPAM/TEST"
test.names <- list.files(test.directory)
training.names <- list.files(training.directory)
library(EML)
library(stringr)
?readLines
?read_lines
?readline
?read_Lines
?read.lines
?readLines
test.directory <- "/Users/jdwilson4/Desktop/CSDMC2010_SPAM/TEST"
test.names <- list.files(test.directory)
test.directory <- "/Users/jdwilson4/Desktop/CSDMC2010_SPAM/TESTING"
test.names <- list.files(test.directory)
readLines(training.names[1])
setwd(training.directory)
readLines(training.names[1])
training.emails <- list()
for(i in 1:length(file.names)){
training.emails[[i]] <- readLines(training.names[1])
}
training.emails[[1]]
class(training.emails[[1]])
str_match(training.emails[[1]], "ofered")
str_match(training.emails[[1]], "explosive")
str_count(training.emails, "explosive")
str_count(training.emails[[1]], "explosive")
str_count(training.emails[[2]], "explosive")
str_count(training.emails[[3]], "explosive")
str_count(training.emails[[100]], "explosive")
for(i in 1:length(file.names)){
training.emails[[i]] <- readLines(training.names[i])
}
sum(str_count(training.emails[[2]]))
sum(str_count(training.emails[[1]]))
str_count(training.emails[[1]])
training.emails[[1]]
sum(str_count(training.emails[[1]], "explosive"))
sum(str_count(training.emails[[2]], "explosive"))
sum(str_count(training.emails[[12]], "explosive"))
sum(str_count(training.emails[[1]], "explosive"))
labels <- readLines("/Users/jdwilson4/Desktop/CSDMC2010_SPAM/SPAMTrain.label")
labels
labels <- read.table("/Users/jdwilson4/Desktop/CSDMC2010_SPAM/SPAMTrain.label")
labels
